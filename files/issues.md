1. why do the hover on edges still say first value pending > what else could we show there instead?

maybe status of connection or some value that makes sense? e.g. a type being parse either a js obj, obj, json, text etc would be good


2. also for icons install and use tabler for all icons used

3. clikcing on the nes example i dont think its applying to auto layout shift L thing properly


4. is how we have our examples the best possible way? should they be json files and have a proper graph loader? 

5. the header looks gross with the new button is doesnt fit the vibe - same with the menu

6. when opening the editor we should shift over the graph view so the editor doesnt cover it up i.e. the node goes in the cneter of the view

7. editor should be word wrapped
